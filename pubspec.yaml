name: docuai
description: "Intelligent document scanner and offline AI assistant. Scanning, OCR, search and answering all run on-device — no account, no backend, no telemetry."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ">=3.1.0 <4.0.0"
  riverpod_annotation: ">=4.0.0 <4.0.5"
  go_router: ^17.3.0
  hive_ce: ^2.19.3
  hive_ce_flutter: ^2.3.4
  path_provider: ^2.1.6
  path: ^1.9.1
  uuid: ^4.6.0
  freezed_annotation: ^3.1.0
  intl: ^0.20.3
  google_mlkit_document_scanner: ^0.5.0
  google_mlkit_text_recognition: ^0.16.0
  pdf: ^3.12.0
  share_plus: ^13.3.0
  image: ^4.8.0
  # Gallery import. On API 33+ this goes through the system Photo Picker and on
  # 24-32 through ACTION_GET_CONTENT — both are system UIs handing back a URI
  # grant, so neither needs a storage permission.
  image_picker: ^1.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  build_runner: ^2.15.1
  freezed: ^3.2.5
  riverpod_generator: ^4.0.3
  hive_ce_generator: ^1.11.0
  flutter_launcher_icons: ^0.14.4

# Launcher icon generation. Run after changing anything in assets/icons/:
#
#   dart run flutter_launcher_icons
#
# The generated files under android/app/src/main/res are committed, so a fresh
# clone builds the branded icon without needing to run this first.
flutter_launcher_icons:
  android: "ic_launcher"
  # iOS has no target in this project — DocuAI is Android-only by design.
  ios: false

  # Legacy square icon for API 24-25, which have no adaptive-icon support.
  image_path: "assets/icons/docuai_icon.png"

  adaptive_icon_background: "assets/icons/background.png"
  adaptive_icon_foreground: "assets/icons/foreground.png"

  # The foreground artwork is a full-bleed composition: its viewfinder brackets
  # run close to the canvas edge. A launcher masks away the outer third of an
  # adaptive icon, so at inset 0 those brackets are simply cut off.
  #
  # 17% on each side scales the artwork into the central 66% — Android's
  # guaranteed-visible area — so the whole composition survives every mask
  # shape. Raising this shrinks the mark; lowering it starts clipping corners.
  adaptive_icon_foreground_inset: 17

  min_sdk_android: 24

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # No `assets:` section on purpose.
  #
  # Everything in assets/icons/ is a *build-time* input to
  # flutter_launcher_icons, not something the running app loads. Declaring the
  # four source PNGs here would bundle roughly 6 MB of artwork into the APK
  # that no widget ever reads — the app draws its launcher icon from the
  # generated mipmaps, which Android supplies.
  #
  # If the mark is ever wanted inside the app — an About screen, an empty
  # state — declare that one file then, at the size it is actually displayed.

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

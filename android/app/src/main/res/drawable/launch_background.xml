<?xml version="1.0" encoding="utf-8"?>
<!--
  Window background shown while the process starts, before Flutter draws.

  Brand ink in both light and dark, rather than following the system theme.
  Committing to one colour means the first frame is always DocuAI instead of
  always the OS, and it removes the white flash a light splash gives dark-mode
  users.

  On Android 12 and above this is largely superseded: the platform composes the
  splash itself from `windowSplashScreenBackground` and the launcher icon. See
  values/styles.xml.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/brand_ink" />
</layer-list>
